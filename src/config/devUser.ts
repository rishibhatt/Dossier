/**
 * Temporary development identity — replace with real session when auth is enabled.
 */
export const devUser = {
  id: "dev-user",
  name: "Demo User",
  email: "demo@dossier.app",
} as const
