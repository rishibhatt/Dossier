/**
 * Canonical Zustand store for portfolio + resolved design config.
 * (Spec name `portfolioStore` — implementation lives in `usePortfolioStore`.)
 */
export { usePortfolioStore } from "@/store/usePortfolioStore"
export type { PortfolioStoreState, PortfolioStylePreferences } from "@/store/usePortfolioStore"
