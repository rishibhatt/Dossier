import { portfolioDocumentSchema } from "@/lib/validations/portfolioDocument"

/** Portfolio document JSON produced by the layout task. */
export const layoutPortfolioSchema = portfolioDocumentSchema
