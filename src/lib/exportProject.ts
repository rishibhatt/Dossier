/**
 * Full Next.js export bundle (Phase 10) — same artifact as `buildPortfolioExportZip`,
 * kept as a dedicated entry for clarity and future expansion (components/, lib/config.ts).
 */
export { buildPortfolioExportZip as buildExportProjectZip } from "@/lib/export/buildPortfolioExportZip"
